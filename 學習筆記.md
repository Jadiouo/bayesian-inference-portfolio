# 貝葉斯推論 · 觀察與學習總整理

> 十個專案做完之後的回頭整理。
> **前半**（§1–§4）是從實作裡長出來的東西，數字都可以在各專案的 `figures/results.json` 查到。
> **後半**（§5）把課程教材接上實作 —— §5.1 的地圖標出 21 張卡片各自對應哪個專案、
> 以及**四個落差**（教材講了但沒實作到）；§5.4 記錄理論預測與實測數字的對照。

- 專案規格與資料來源：[`貝葉斯推論_實作訓練計劃.md`](貝葉斯推論_實作訓練計劃.md)
- 課程教材：[`貝葉斯推論教材/00_課程總覽.md`](貝葉斯推論教材/00_課程總覽.md)（副本，正本在 Obsidian vault）
- 各專案完整敘事：每個資料夾的 `README.md`

---

## 1 · 一頁總覽

| 專案 | 領域 | 一句話核心觀察 |
|---|---|---|
| [D1](D1_ab_testing/README.md) | 商業 A/B | 貝葉斯**不是**「偷看不會出事」的萬靈丹 —— 配對門檻下後驗一樣膨脹 |
| [A1](A1_diagnostic_decision/README.md) | 醫療決策 | 後驗只是中間產物，**損失矩陣才決定門檻**；同一個後驗換成本假設就換答案 |
| [C1](C1_hierarchical_pooling/README.md) | 階層模型 | 收縮量 ω 隨樣本數的變化**緊貼理論的精確度加權曲線** —— 部分匯聚不是玄學 |
| [B1](B1_transit_fitting/README.md) | 天文參數估計 | 儀器效應（長曝光積分）沒建模，會**系統性地把物理參數壓低**而擬合看起來仍好 |
| [B2](B2_bayes_factor/README.md) | 模型比較 | **先驗範圍不是旋鈕，就是模型定義**；奧卡姆剃刀在 logZ 裡是可測量的 |
| [A2](A2_survival_analysis/README.md) | 生存分析 | **用後驗區間寬度當品質指標，會挑到最錯的那一個**（最窄的偏差 83%）|
| [A3](A3_image_uncertainty_ood/README.md) | 影像 OOD | 模型對 FashionMNIST 比對真實胸片更自信；**trivial 像素統計 4 組贏 3 組** |
| [E1](E1_bayesian_optimization/README.md) | 實驗設計 | 高維失敗的機制**不是**內層優化解不動，是 **GP 退化成先驗**、acquisition 平坦化 |
| [C2](C2_epidemic_sir/README.md) | 疾病傳播 | **模型失配會偽裝成參數估計**：固定 β 收斂完美卻要 86.8% 感染率 |
| [S1](S1_vae_anomaly/README.md) | 變分推論 | ELBO 一大半在測「影像多難壓縮」；**兩行 numpy 打敗 VAE 16 個百分點** |

---

## 2 · 貫穿全作品集的六條主線

這一節是整份文件最該記住的部分。十個專案分屬五個領域、用了六種推論工具，
但真正重複出現的教訓只有六條。

### 2.1 每個結論都需要一個「模型之外」的檢查

**沒有對照的結果，說服力接近零** —— 而且往往是**錯的**。

| 專案 | 外部檢查 | 檢查前的結論 | 檢查後 |
|---|---|---|---|
| A3 | 不看模型的像素統計 | 「模型偵測到 OOD」 | trivial 4 組贏 3 組 |
| S1 | 同上 + PNG 壓縮位元數 | 「ELBO 偵測到病灶」 | ELBO 大半在測複雜度 |
| A2 | AFT→PH 換算對上 Cox | 手刻刪失似然「應該對」 | 平均 \|差\| 0.0078 ✓ |
| B1 | 對上已發表的 Rp | 擬合品質很好 | 曾系統性壓低 Rp（缺長曝光積分）|
| C2 | 兩個資料來源交叉驗證 | OWID 可以用 | OWID 已改週報格式，85% 的天是 0 |
| E1 | vs sklearn + 解析梯度 vs 差分 | 手刻 GP「跑得動」 | rel err 2–3e-8 ✓ |

**關鍵**：檢查必須**獨立於**被檢查的東西。用模型的另一個輸出檢查模型是循環論證。

### 2.2 收斂診斷通過 ≠ 答案對

C2 的固定 β SIR：**0 divergences、r_hat 1.000、ESS 1923** —— 教科書級的收斂。
得到的解是 86.8% 的人口被感染、通報率 0.28%。

機制：固定 β 時 $R_t=(\beta/\gamma)(S/N)$ **只能靠耗盡易感者下降**，
要在三個月做到，就得感染 86.8% 人口，再用 0.28% 的通報率把量級壓回來。
兩個模型都能擬合曲線，**只看擬合品質完全抓不到**。

同類：A2 的 PyMC 預設 `init="jitter+adapt_diag"` 讓單一鏈整條卡死，
**只影響部分 seed**，單跑一次不會發現，但在多 seed 實驗裡會偽裝成漂亮趨勢。

> **教訓**：收斂診斷檢查的是「取樣器有沒有正確地探索**這個模型**的後驗」，
> 完全不檢查「這個模型是不是對的」。後者只能靠後驗預測檢查、
> 樣本外預測、以及參數值的物理合理性。

### 2.3 指標必須對上你要回答的問題

| 專案 | 兩個指標打架 | 為什麼 |
|---|---|---|
| A2 | WAIC 排開 4.8σ，但 OOF C-index 三模型全在 0.691–0.693 | WAIC 測「密度預測」，C-index 測「排序」|
| S1 | β=16 的 val ELBO 差 11 nats，AUROC 反而最高 | ELBO 測生成品質，AUROC 測偵測能力 |
| A3 | epistemic 是理論上「該用」的 OOD 分數，總熵實際更好 | OOD 主要抬升 **aleatoric**（見 2.5）|
| A2 | 「最窄的後驗」= 最偏的那個（偏差 83%）| 寬度測精度，不測偏差 |

**先問「我要回答什麼」，再選指標**。反過來做（先算一堆指標再挑好看的）
是這十個專案裡最容易犯、也最難自己發現的錯。

### 2.4 先驗的角色隨任務而完全不同

同樣是「放寬先驗」這個動作：

| 任務 | 專案 | 效果 |
|---|---|---|
| 參數估計 | A2 | 先驗放寬 **8 倍**，β_pnodes 只移 **2.78%** |
| 模型比較 | B2 | 先驗放寬 **3 倍**，貝氏因子**翻盤** |

機制：邊際似然 $Z=\int p(D|\theta)p(\theta)d\theta$ **對先驗的體積直接敏感**
（那正是奧卡姆剃刀的來源），而後驗 $p(\theta|D)\propto p(D|\theta)p(\theta)$
在資料充足時會把弱先驗洗掉。

B2 還發現一個更深的版本：Kepler-10b 完美貼合 Occam 預測的 $-\log_{10}(k)$，
但 KOI-6017 **反向上升** —— 因為它的 M1 把 rp 頂在先驗邊界，放寬邊界讓 M1 找到更好的解，
似然增益蓋過體積稀釋。**所以先驗範圍不是旋鈕，就是模型定義的一部分。**

### 2.5 不確定性的分解要驗證，不能宣稱

A3 的核心方法論結果：
- 加噪聲 → aleatoric 上升 ✓（選擇性 mcdrop 3.50 / ens 1.20）
- 換分佈 → epistemic 上升 **✗**（0.30 / 0.76）

**OOD 主要抬升的是 aleatoric** —— 模型把「沒見過」誤記成「圖本身模糊」。
這解釋了為何 epistemic 當 OOD 分數效果有限，也解釋了 risk-coverage 上總熵反而更好。

> 「aleatoric = 資料噪聲、epistemic = 模型無知」是**定義**，不是**保證**。
> 要證明你算出來的兩個數字真的對應這兩件事，得做受控實驗：
> 分別注入已知的噪聲與已知的分佈偏移，看哪一項動。

### 2.6 只設 seed 不足以可重現

A3 實測：兩次「相同」執行的 ensemble 準確率 **0.885 vs 0.901**。
cuDNN 的非決定性累積後改變了 early stopping 的選點。

完整配方：
```python
os.environ["CUBLAS_WORKSPACE_CONFIG"] = ":4096:8"   # 必須在 import torch 之前
torch.backends.cudnn.deterministic = True
torch.backends.cudnn.benchmark = False
torch.use_deterministic_algorithms(True)
```
之後兩次完整執行每個數字逐位一致（S1 的 notebook 與 `run_all.py` 也驗證了這點）。

**推論**：任何「調了 X 之後變好了」的宣稱，都要先建**噪聲底線**。
S1 用 5 個 seed 得到 AUROC 0.7573 ± **0.0026**，於是 β 掃描的 0.02 全距
才能被判讀成 7.8σ（真實但小），IWAE 的 0.0005 才能被判讀成 0.2σ（雜訊）。

---

## 3 · 每個專案的觀察與學習

### D1 · 貝葉斯 A/B 測試

**核心觀察**
- P(B>A) = **88%** vs 頻率派 p = 0.24 —— 同一份資料，兩個問題，兩個答案。
  頻率派問「假設沒差別，看到這麼極端的資料有多罕見」；貝葉斯直接問「B 比較好的機率」。
- **偷看假陽性 5% → 26%**。
- 但**誠實展示**：在配對門檻下（P>0.975 ↔ α=0.05），貝葉斯後驗**一樣膨脹**。

**學到什麼**
- 「貝葉斯不受多重比較影響」這個常見說法，在**固定決策門檻**的實務用法下不成立。
  真正免疫的是「不設門檻、只報後驗」——但那不是 A/B 測試在做的事。
- 講一個方法的優點時，要把它的**適用條件**一起講。這比多報一個好數字更有說服力。

**技術**：偷看模擬用常態近似向量化預計算加速。

---

### A1 · 診斷決策系統

**核心觀察**
- 損失矩陣 100:1（漏診:誤診）→ 最優門檻 **0.01**，不是預設的 0.5。
- 20:1 的情況下，用 0.5 當門檻的期望損失比最優**高 86%**。
- 後驗預測 vs plug-in：Jensen 不等式把機率**拉向 0.5**。
- 校準誠實面：5-fold OOF ECE 貝葉斯 **0.045** ≈ 頻率派 **0.047** —— 相近，不誇大。

**學到什麼**
- **後驗只是中間產物**。「90% 惡性」不是答案，「該不該開刀」才是，
  而中間那一步需要一個明確寫下來的損失函數。
- 把成本假設寫成可調參數，讓讀者自己看結論怎麼隨假設變 —— 比爭論「正確的成本比是多少」有用得多。
- 棄權（abstain）選項是決策理論的自然延伸，不是額外的工程 trick。

**踩坑**：pandas `dropna()` 後要 `reset_index`，否則 `pd.concat(axis=1)` 對不齊。

---

### C1 · 階層模型與部分匯聚

**核心觀察**
- 收縮圖是靈魂：ω 隨 n 從 **0.64 降到 0.14**，且**緊貼理論的精確度加權曲線** $\sigma^2/(\sigma^2+n\sigma_a^2)$。
- $\sigma_a = 0.319$ [0.241, 0.417] 明確 > 0 —— 郡之間確實有差異，資料自己說的。
- 預測（10 次切分平均）：hier 0.765 < complete 0.791 < no-pool 0.797，**小郡優勢最大**。
- Funnel 示範另用 Eight Schools：centered **113 divergences** vs non-centered **0**。

**學到什麼**
- 部分匯聚**不是**「取個折衷」的啟發式，它是模型結構的必然推論，
  而且收縮量有解析形式可以對照。這是整個作品集裡「理論與實作對得最準」的一次。
- **選對展示用的資料集**：radon 資料多、funnel 不明顯，硬用它示範會看不到重點。
  換 Eight Schools 不是取巧，是讓現象在它最清楚的地方被看見。
- 非中心化參數化不是玄學調參，它是對同一個模型的**重新座標化**，後驗分佈不變、幾何變好。

---

### B1 · 凌日光曲線參數估計

**核心觀察**
- BLS 週期 P = **0.837487** 天，對上已發表的 0.837491 —— 差 **4 ppm**。
- Rp = **1.50 R⊕** [1.46, 1.59]，涵蓋已發表的 1.47。
- **強簡併**：corr(rp, a) = −0.86、corr(a, b) = **−0.94** —— Mean-Field VI 在這裡會失敗。
- 誠實面：a/R* ≈ 4.2 偏高（簡併 + 無星震密度先驗），但物理半徑涵蓋。

**學到什麼**
- **儀器效應必須進模型**。batman 沒對長曝光積分（29.4 分鐘）時，
  擬合會用極端的 limb darkening 去磨圓凹陷底部，**系統性壓低 Rp/R\***
  —— 而擬合品質看起來仍然很好。這是 2.1 那條主線最乾淨的例子。
- 後驗的**形狀**和後驗的**位置**一樣重要。香蕉形後驗告訴你參數組合有簡併，
  這是物理資訊（凌日深度約束的是 rp 與 a 的組合，不是各自）。
- 資料不約束的東西，就用外部知識當強先驗（LD 用恆星模型理論值），並說清楚。

**踩坑**：(a) 長曝光積分 supersample + exp_time；(b) 香蕉形後驗要用 emcee 的
**DEMove** 而非預設 StretchMove；(c) 淺凌日不約束 LD。

---

### B2 · 貝氏因子「這是不是行星」

**核心觀察**
- Kepler-10b：$\log_{10}B(M_1/M_0) = +167.45$；$\log_{10}B(M_2/M_1) = \mathbf{-1.30}$。
- **奧卡姆剃刀現場**：M2（食雙星）的 max-likelihood 高 **+2.7**，logZ 卻低 **2.98**
  —— 多出來的參數體積被懲罰掉了。
- **ΔBIC = −0.30 判到反邊** → 邊緣案例上 BIC 近似不可靠。
- KOI-6017：$\log_{10}B(M_2/M_1) = \mathbf{+9.39}$ 決定性判食雙星。
- Injection–recovery 調到 5.3 ppm（$\log_{10}B=+0.35$ 剛好在邊界）時，
  **先驗只放寬 3 倍就翻盤** —— 這才是 Lindley 悖論真正咬人的地方。

**學到什麼**
- 貝氏因子懲罰的是**先驗預測的體積**，不是參數個數。BIC 用參數個數近似它，
  在邊緣案例上會失效。
- **選對照組要選對**：用官方 `koi_fpflag_ss=1` + `DEEP_V_SHAPED` + `HAS_SEC_TCE` 篩，
  等於挑一個「官方判它假的理由，正好對上我的 M2 要抓的特徵」的案例。
- 敏感度分析不是形式，是會改變結論的。

**踩坑**：(a) **對稱 `remove_outliers(sigma=5)` 會把 2% 深的食當離群值砍掉**（實測砍 1391 點全在食內）；
(b) 摺疊要覆蓋完整相位 [−0.5, 0.5] 才看得到次食；
(c) **ultranest 預設 MLFriends 在 8 維跑 12 分鐘不收斂，換 `SliceSampler` 後 63 秒**；
(d) logZ 差距小時 `logzerr` **不含** step sampler 的偏差，要另做 nsteps 階梯檢查。

---

### A2 · 貝葉斯生存分析

**核心觀察**
- 通關 1：正確處理刪失中位存活 **4.62 年**（KM 4.95，−6.6%）
  vs 刪失當事件 **2.64**（−46.7%）vs 丟掉刪失 **2.17**（−56.2%）。
- 通關 3：WAIC 選 Log-Normal（Δelpd 20.65，dse 4.26 → **4.8σ**）。
- **但 OOF C-index 三模型加 Cox 全在 0.691–0.693**，配對 bootstrap ΔC = +0.0017 [−0.0031, +0.0066]。
- AFT→PH 換算 $\beta_{PH} = -k\beta_{AFT}$ 與 Cox 平均 |差| 僅 **0.0078**。

**兩個被推翻的假設**（這是本專案最有價值的部分）
- **「精度只由事件數決定」被推翻**。三臂實驗：A（截短追蹤期）比 B（分層子抽樣）窄 **17.9%**，
  log-log 斜率 A −0.250 vs B −0.547（只有 B 貼合理論 −0.5）。
  機制：A 保留全部特徵向量、只把結果粗化成「T > h」，
  **迴歸係數的精度吃的是協變量變異度**，不只是事件數。
- **「丟掉刪失最寬」被推翻得更有價值**。C 臂的後驗**最窄**（sd 0.0309）但偏差 **83.1%**
  （A 13.0% / B 3.5%），「最窄且最偏」在 8/8 設定成立。
  因為 C 只留下早期發生事件的人 → **對一個有偏的樣本很有信心**。

**學到什麼**
- 這是 2.3 那條主線的極端版本：**用後驗區間寬度當模型品質指標，會挑到最錯的那一個。**
- 刪失不是「缺失資料」，它是**部分資訊**（我們知道 T > c）。丟掉它偏 −56%，
  當成事件偏 −47%，正確處理只偏 −6.6%。
- 「兩個實作互相驗證」（AFT vs Cox）比「一個實作看起來合理」強得多。

**踩坑**：(a) `init="jitter+adapt_diag"` 讓單一鏈卡死（見 2.2）；
(b) **拿個體風險 h(t|x̄) 對 Nelson–Aalen 會誤判失配** —— 該比的是族群層級
$h_{pop}(t)=\sum S_ih_i/\sum S_i$（frailty 效應：高風險者先離開風險集合 → 族群峰值更早）。
實測個體 3.09 年 vs 族群 2.04 年 vs 非參數 1.40 年；
(c) Nelson–Aalen 尾端是噪聲，會長出假的「第二個高峰」（t=5.5 年只剩 74 人在風險中）。

---

### A3 · 影像不確定性與 OOD

**核心觀察（主要結論是負面的，而那是最有價值的部分）**
- **模型對 FashionMNIST 比對真實胸片更自信**（0.9845 vs 0.9444），AUROC 僅 **0.552**；
  而 trivial 的影像 L2 norm 達 **0.999**。機制：ReLU 網路在遠離資料處任意自信（Hein et al. 2019）。
- **trivial 像素統計 4 組中 3 組打敗模型**（Derma 通道差異 **1.000 完美**）。
- **唯一模型勝出的是我自己加的「灰階受控組」**（0.849 vs 0.780）——
  把顏色捷徑拔掉後 trivial 從 1.000 掉到 0.780，模型只從 0.867 掉到 0.849。
- **通關標準「難度遞增」被推翻**：實際 Derma 0.867 > 灰階 0.849 > 留出類 0.770 > **Fashion 0.552**。
- **非單調反轉**：噪聲 σ>0.5 後不確定性**下降**而準確率繼續掉 = 自信地錯誤。

**學到什麼**
- **加一個受控組，才能把「模型會不會」和「資料集有沒有捷徑」分開。**
  灰階組是整個 A3 最關鍵的設計 —— 沒有它，就只能說「trivial 贏了」，
  不能說「但模型用的確實不是顏色」。
- OOD 的難度**不由語意距離決定**，而由「OOD 落在網路的哪個區域」決定。
- 量測位置會影響結論：第一版用最大 σ 算選擇性，落在反轉之後，已改在 aleatoric 峰值量測。

**踩坑**：(a) **`model.train()` 會連帶開 BatchNorm** → 預測依賴同 batch 其他樣本
+ 每次 MC 前向更新 running stats（**測試集洩漏**）。改用 GroupNorm + `enable_dropout_only()`；
(b) **batch 組成不變性檢查必須在 CPU 上做** —— CUDA 的 cuDNN 依 batch size 選不同卷積演算法，
即使無 batch 依賴也有 ~1e-4 浮點差異（CUDA 1.7e-4 vs CPU 6.3e-7）；
(c) 只設 seed 不足以可重現（見 2.6）。

---

### E1 · 從零實作貝葉斯最優化

**核心觀察**
- 通關 1：Branin BO-EI **0.447 ± 0.077** vs random 1.995 ± 1.511（**sd 小 20 倍**）。
- 通關 2：κ 掃描出現完美 U 型。κ=0 的**不重複點比例僅 0.516** —— 一半的評估花在幾乎相同的位置。
- 通關 3：配對優勢從 5D +13.66 衰減到 **50D +1.77 ± 1.92（區間涵蓋 0）→ 臨界維度 50D**。
- 真實應用：Concrete 上 **BO 只用 14 次達到 random 花光 40 次的水準 → 省 65%**。

**一個被否證的機制假設**
- 我原以為高維失敗來自 acquisition **內層優化**解不動。實測 shortfall ≈ 0
  （50 個候選點與 20000 個找到相同值）。
- 真正原因是 **GP 退化成先驗、acquisition 平坦化**：後驗均值的動態範圍 ÷ y 範圍
  從 2D 的 1.008 崩到 50D 的 **0.016**。配套診斷：「有效鄰居」(k>0.1) 比例 68.6% → **0%（20D 起）**。
- **兩者的補救方式完全不同**（加大內層預算 vs 換模型），所以區分有實際價值。
- ⚠️ 必須除以 y 範圍正規化 —— Ackley 的值域本身隨維度縮小，不正規化會把尺度效應誤讀成模型退化。

**學到什麼**
- 「方法在高維失效」是現象，**機制**才是可行動的知識。診斷到機制之前，任何改進都是盲調。
- 探索–利用的 trade-off 可以被完全量化（κ 的 U 型 + 不重複點比例 + 探索半徑）。
- Matérn 5/2 學到的 ℓ 是 RBF 的 **2.3 倍**（0.745 vs 0.322）——
  RBF 假設無限可微，在崎嶇目標上被迫用短 ℓ，後驗在觀測之間迅速回到先驗。核的選擇是有後果的建模決定。

**踩坑**：(a) **Concrete 代理模型的 CV 沒 shuffle** —— 該資料集有排序結構，
`cross_val_score(cv=5)` 預設不 shuffle 給出 R² = **0.43 ± 0.67**，改 `KFold(shuffle=True)` 後 **0.935 ± 0.011**；
(b) Thompson sampling 慢 45 倍（聯合抽樣要 O(m³) Cholesky）；
(c) **log y 軸 + 對稱誤差帶會塌陷**（mean−sd 為負，matplotlib 把下緣塌到軸底遮蔽所有曲線）。

---

### C2 · 疾病傳播 SIR

**核心觀察**
- **固定 β 收斂完美卻得到荒謬解**（見 2.2）：I0 = 483,755、ρ = 0.0028、累計感染 **86.81%**。
  時變 β 則 I0 = 665 [245, 2396]、ρ = 0.207、累計感染 **1.06%** —— 全部落在文獻範圍。WAIC 差 106.15。
- 通關 1：全國接觸限制（3/22）後 $R_t$ **3.20 → 2.15**，P(下降) = **1.000**；
  **2020-04-11 首次 P($R_t$<1) > 0.95**。因累計感染僅 1.06%，S/N ≈ 1
  → **$R_t$ 的變化幾乎全來自 β（行為/政策），不是群體免疫**。
- 週末效應（決定性）：殘差 ACF 在 lag 7/14/21，有 dow 項 **0/3 顯著**，無 dow 項 **3/3 全顯著**。
- 通關 3（部分）：樣本外 95% 帶覆蓋 0.857，但 **50%/80% 帶嚴重不足（0.286/0.500）**
  → random walk 外推**無漂移項**，低估了鬆綁後的回升。

**學到什麼**
- **資料來源要驗證，不能假設**。計劃書指定的 OWID 已回溯改成**週報格式**，
  德國 2020-03~06 有 **85% 的天是 0**。換 JHU CSSE 後用**週總和交叉驗證**：
  總量相對差 **0.04%**、週相關 0.9792 → 同一場疫情只差解析度。
- 時間序列的**逐點 LOO 概念上不適用**（max Pareto k = 0.74 > 0.7）：
  留一個時間點出來、鄰居仍在且 random walk 讓鄰居攜帶幾乎全部資訊 → LOO 系統性高估。
  「能否預測未來」只能由**向前樣本外**檢查回答。
- 相關 ≠ 因果要明說：行為改變常早於法令、多措施數天內連發。

**踩坑**：(a) **`pytensor.scan` 的 `outputs_info` 必須與 `sequences` 同 dtype**，
錯誤訊息指向 scan 內部、完全看不出根因；
(b) **通關 2 的度量第一版寫錯** —— 用 $R_t$ 區間的**絕對**寬度得到方向相反的結論，
因為 $R_t$ 早期 3.2 / 尾端 0.6 差 5 倍，絕對寬度跟著**乘法尺度**縮放。必須用**相對**寬度 log(hi/lo)；
(c) 交叉驗證指標用 |Δ|/OWID 在某週趨零時爆出 **760 倍**無意義值，改用對稱 SMAPE。

---

### S1 · VAE 異常偵測

**核心觀察**
- 通關成立：−ELBO AUROC **0.7555**，Cohen's d −0.90 —— **但重疊係數 0.57**（「明顯分離」過頭了）。
- 分解：判別力幾乎全來自重建項；**KL 項 AUROC 0.3814 < 0.5 反向**
  （異常的 latent 反而**更靠近**先驗）。機制：VAE 容量有限只學到低頻結構，
  肺炎片本身更糊更平坦，**反而更接近 VAE 能表達的東西**。
- **trivial 打敗模型**：gradient energy **0.9147**（6 個統計有 4 個贏）。
- 追查：Spearman(ELBO, PNG bits) = +0.447（合併）；分層 AUROC 0.7555 → **0.6664**，
  殘餘底線 0.542 → 淨貢獻僅 **+0.12**。
- 修好它：$S = -\mathrm{ELBO} - L(x)$ → **0.8808**（配對 Δ +0.125，勝率 100%）。

**四個負面 / 意外結果**（seed 底線 0.7573 ± 0.0026）
- **IWAE 收緊下界完全無效**：K=1→100 下界漲 0.6 nats，AUROC 全距 0.0005（**0.2σ**）。
  因為收緊量相對 ELBO 的 −496 量級只有 0.1%。
- **β=16 完全 collapse（0 個活躍單元）AUROC 反而最高 0.762**，而該模型 val ELBO 差 11 nats。
- latent 容量 d ≥ 8 即飽和，擴到 64 活躍單元只到 17（沒出現「連異常也重建出來」的擔憂）。
- 效應量排序：**複雜度校正 48.2σ ≫ latent 15.3σ > β 7.8σ > IWAE 0.2σ**。

**學到什麼**
- **通關標準會說謊**。「異常的 ELBO 明顯較低」是真的，只做到這裡會是一個看起來成功的專案。
  加一個兩行的 baseline，結論就翻轉了。
- **知道分數在測什麼，比讓分數變高更有價值**。複雜度校正之所以有效（遠超所有超參數），
  正因為它針對的是**已診斷出來的**失效機制，而不是盲調。
- 分層 AUROC 的殘餘底線 0.542 不是 0.5 —— **當對照底線報告，不藏起來**。

**踩坑**：(a) **KL 驗證第一版「失敗」是測試設計錯** ——
拿未訓練模型的輸出測，μ≈0/logvar≈0 使 KL≈0.003，相對誤差分母趨零、MC 噪聲讓誤差爆到 1.1。
改成抽受控 (μ, logvar) 讓 KL 落在 10–49 nats，**容差改用 MC 自己的標準誤**；
(b) 梯度變異數比較要兩邊用**同一組 ε** 且 REINFORCE 要加 baseline（仍差 **898×**）；
(c) 分數的重建項要用 8 個 MC 樣本平均，單樣本噪聲會直接稀釋 AUROC。

---

## 4 · 七個主題 × 專案對照（從實作回頭對理論）

| 主題 | 做到它的專案 | 實作裡它長什麼樣 |
|---|---|---|
| **一 · 貝葉斯定理 / 序貫更新** | D1, A1, B1, C1, C2, E1 | E1 最純粹：每次評估後更新 GP 後驗，再用它決定下一點 |
| **二 · 先驗設計** | 全部（除 A3, S1）| B1 的 LD 強先驗（資料不約束就用外部知識）；B2 的先驗範圍 = 模型定義 |
| **三 · 後驗解讀 / 可信區間** | A1, A2, B1, C1, C2, D1 | C2 的 $R_t$ 可信帶 + P($R_t$<1) 的日期；D1 的 P(B>A) |
| **四 · 近似推論（MCMC）** | A1, A2, B1, B2, C1, C2 | B1 用 emcee（DEMove 對付香蕉形）；B2 用 nested sampling 算 logZ |
| **五 · 變分推論 / ELBO** | **S1**（主軸）, A3 | S1 的 ELBO 分解 + IWAE + 重參數化 vs REINFORCE |
| **六 · 不確定性分解** | A3, S1, A1, C2, E1 | A3 的 aleatoric/epistemic 受控驗證（並發現分解名不符實）|
| **七 · 決策 / 最優化 / 模型選擇** | A1, A2, A3, B2, D1, E1 | A1 的損失矩陣→門檻；B2 的貝氏因子；E1 的 acquisition |

**六種推論工具，各自的適用邊界**（做完才知道的）：

| 工具 | 用在 | 什麼時候會失敗 |
|---|---|---|
| PyMC / NUTS | A1, A2, C1, C2 | 參數數量隨資料量增長時（S1 的 1214 個 latent）|
| emcee | B1 | 高維；預設 StretchMove 對付不了強簡併 |
| ultranest（nested） | B2 | d ≳ 7 要換 step sampler，否則不收斂 |
| GP 解析後驗 | E1 | 高維（20D 起有效鄰居 0%）|
| 變分推論 | S1 | 近似品質不可知；Mean-Field 遇強簡併會失敗（B1 的例子）|
| Deep Ensemble / MC Dropout | A3 | 遠離資料處任意自信（ReLU 網路的結構性問題）|

---

## 5 · 課程教材 × 實作的對照

> **教材位置**：[`貝葉斯推論教材/`](貝葉斯推論教材/00_課程總覽.md)（21 張卡片，七個主題 + 3 附錄）。
> 教材是 Obsidian 格式（`[[wikilink]]`、frontmatter、callout、mermaid 依賴圖），
> 在 Obsidian 裡讀最完整 —— 卡片間的連結可點、依賴圖會渲染。
> 在 GitHub 上 wikilink 會顯示成純文字，但內容完整可讀。
>
> 建議的用法：
> 1. 上完一張卡片，回 **§5.2** 填「這個概念在哪個專案出現過」。
> 2. **§5.4** 是最有價值的一格 —— 課堂說法與實作結果不一致的地方。
>    通常不是誰錯了，而是**適用條件不同**，那裡是真正的理解發生的地方。已預填 4 組。
> 3. **§5.5** 記讀卡片時的筆記與疑問。

### 5.1 教材地圖 × 我的專案

七個主題與計劃書 §7 **一一對應**。下表把 21 張卡片接到實作上：

| 主題 | 卡片 | 對應專案 | 實作裡它長什麼樣 |
|---|---|---|---|
| **一 · 貝葉斯定理的真正意義** | 1-1 四個項的認識論意義 | 全部 | — |
| | 1-2 序貫更新與 Kalman Filter | E1 | 每次評估後更新 GP，再決定下一點 |
| | 1-3 邊際似然的困境 | **B2** | 正面迎擊：用 nested sampling 直接算 $\log Z$ |
| **二 · 先驗的設計哲學** | 2-1 無資訊先驗與 Jeffreys | A1, A2 | 標準化後 β~N(0, 2.5) 的弱資訊先驗 |
| | 2-2 共軛先驗與指數族 | **D1** | Beta–Binomial 封閉解，不必 MCMC |
| | 2-3 最大熵先驗 | — | ⚠️ **落差**：沒有專案明確用最大熵推導先驗 |
| **三 · 後驗的幾何與解讀** | 3-1 點估計與損失函數 | **A1** | 損失矩陣 100:1 → 最優門檻 0.01（不是 0.5）|
| | 3-2 後驗預測分佈 | **A1** | 後驗預測 vs plug-in：Jensen 把機率拉向 0.5 |
| | 3-3 可信區間 vs 信賴區間 | **D1** | P(B>A)=88% vs 頻率派 p=0.24 |
| **四 · 精確與近似後驗** | 4-1 Laplace 近似 | — | ⚠️ **落差**：完全沒實作過 |
| | 4-2 VI 與 MCMC 兩條路 | 全部 | 九個專案走 MCMC/nested/GP，**只有 S1 走 VI** |
| | 4-3 MCMC 的核心直覺 | A1, A2, C1, C2 | NUTS；B1 用 emcee，B2 用 nested |
| **五 · 變分推論的完整推導** | 5-1 ELBO 的第一原理推導 | **S1** | ELBO 分解成重建項 + KL，各自驗證 |
| | 5-2 MeanField 與 CAVI | — / B1 | ⚠️ **落差**：沒實作 CAVI（S1 是 amortized VI + SGD）|
| | 5-3 重參數化技巧 | **S1** | vs REINFORCE 實測梯度變異數差 **898×** |
| **六 · 貝葉斯深度學習** | 6-1 貝葉斯神經網路 | A3 | Deep Ensemble 當近似 BMA |
| | 6-2 MC Dropout | **A3** | T=50；踩到 BatchNorm 污染的坑 |
| | 6-3 兩種不確定性 | **A3, S1** | 分解**驗證失敗**：OOD 主要抬升 aleatoric |
| **七 · 貝葉斯的決策理論** | 7-1 貝葉斯決策框架 | **A1** | 後驗 → 損失 → 行動，含棄權選項 |
| | 7-2 貝葉斯最優化 | **E1** | 手刻 GP + 三種 acquisition；臨界維度 50D |
| | 7-3 貝葉斯模型選擇 | **B2** | 貝氏因子；奧卡姆剃刀在 logZ 裡可測量 |

**四個落差**（教材講了、實作沒碰到）：2-3 最大熵先驗、4-1 Laplace 近似、
5-2 CAVI、1-2 Kalman Filter。前兩個最值得補（見 §5.6）。

---

### 5.2 課堂概念 → 我的實作

<!-- 上完一張卡片就回來填一列。左邊照抄教材用詞，右邊寫我在哪裡遇到它。 -->

教材 00 總覽有一張「你以為是獨立的 → 其實是」表，其中**六項直接對上我的專案**，
先填在這裡：

| 教材說「其實是」 | 我的專案 | 實作裡的證據 |
|---|---|---|
| VAE = VI + 重參數化 | S1 | 整個專案就是這句話的展開 |
| Dropout = 廉價的變分推論 | A3 | MC Dropout T=50 當 epistemic 來源 |
| Ensemble = 貝葉斯模型平均 | A3 | Deep Ensemble M=5，多數情況優於 MC Dropout |
| BIC / MDL = 邊際似然的近似 | **B2** | **ΔBIC=−0.30 判到反邊**，證明近似在邊緣案例不可靠 |
| Bayesian Optimization = 序貫更新 + 決策理論 | E1 | acquisition 就是決策規則 |
| ROC / CFAR = 貝葉斯決策的二元特例 | A1 | 損失矩陣決定門檻，ROC 上的操作點 |

<!-- 以下自己往下加 -->

| 課堂概念 / 用詞 | 出現在哪個專案 | 我實際遇到的樣子 | 備註 |
|---|---|---|---|
| | | | |
| | | | |

---

### 5.3 課程進度追蹤

<!-- 讀到哪張卡片就打勾，順便記下讀的日期與心得指標。 -->

| 主題 | 卡片 | 讀了 | 日期 | 我的筆記在哪 |
|---|---|:-:|---|---|
| 一 | 1-1 / 1-2 / 1-3 | ⬜⬜⬜ | | |
| 二 | 2-1 / 2-2 / 2-3 | ⬜⬜⬜ | | |
| 三 | 3-1 / 3-2 / 3-3 | ⬜⬜⬜ | | |
| 四 | 4-1 / 4-2 / 4-3 | ⬜⬜⬜ | | |
| 五 | 5-1 / 5-2 / 5-3 | ⬜⬜⬜ | | |
| 六 | 6-1 / 6-2 / 6-3 | ⬜⬜⬜ | | |
| 七 | 7-1 / 7-2 / 7-3 | ⬜⬜⬜ | | |
| 附錄 | A1 符號工具箱 / A2 三個神問 / A3 概念索引 | ⬜⬜⬜ | | |

---

### 5.4 課堂 vs 實作：理論預測 ↔ 實測數字

**這一格最值得慢慢填。** 四組已驗證的對照 —— 教材給機制，我的專案給數字：

| 教材說什麼 | 我的實測 | 關係 |
|---|---|---|
| **5-3**：REINFORCE 是替代品，「變異數大、訓練不穩」 | S1 量出 **898×**（同一組 ε、REINFORCE 還加了 baseline）| ✅ **證實**，並把定性說法變成一個數字 |
| **5-2**：「變數強相關 → Mean Field 不可接受」，會嚴重低估不確定性 | B1 的凌日後驗 corr(rp,a)=−0.86、**corr(a,b)=−0.94** | ✅ **證實**。這也解釋了 B1 為何用 emcee 而非 VI |
| **6-3**：epistemic 對應「模型沒見過」，是 OOD 的訊號 | A3 受控實驗：換分佈時 epistemic 選擇性只有 **0.30/0.76**，OOD 主要抬升 **aleatoric** | ⚠️ **部分不成立**。定義是對的，但**實際算出來的量**沒對應到定義 |
| **7-3**：BIC / MDL 是邊際似然的近似 | B2 的 ΔBIC = **−0.30**，與 $\log_{10}B$ = −1.30 **判到反邊** | ⚠️ **近似會失效**。差距小時不能用 BIC 代替 logZ |

<!-- 往下加。特別留意這三種情況：
     (a) 教材說「通常會怎樣」，我實測是相反的
     (b) 教材的條件我沒滿足（例如 Mean Field 的獨立性假設）
     (c) 教材沒提到的失效模式（例如 A3 的 ReLU 網路在遠處任意自信） -->

| 教材說什麼 | 我的實測 | 關係 |
|---|---|---|
| | | |
| | | |

---

### 5.5 讀教材時的筆記 / 疑問

<!-- 讀卡片時想到的東西往這裡放。建議每張卡片開一個小節。 -->

#### （卡片編號）· （日期）

-

**待查**

- [ ]
- [ ]

---

### 5.6 想補做的實驗

<!-- 各專案 README 的「限制與誠實的部分」列了不少沒做的東西，
     上課如果講到相關的方法，可以回來挑一個補。 -->

**補教材落差的**（§5.1 找出的四個，教材講了但實作沒碰到）：

| 想做什麼 | 補哪張卡片 | 最省力的接法 |
|---|---|---|
| **Laplace 近似** | 4-1 | 拿 **A1** 的邏輯迴歸後驗，比較 Laplace vs NUTS vs plug-in 三者的預測機率與 ECE。已有現成資料與 NUTS 基準，只要多寫一個 Hessian |
| **最大熵先驗** | 2-3 | 在 **C2** 用最大熵推導 β 的先驗（已知均值與範圍的約束下），對照現在用的 random walk |
| **CAVI** | 5-2 | 在 **C1** 的階層模型上手刻 CAVI，對照 NUTS 的後驗 —— 順便實測 Mean Field 對階層模型低估多少 |
| **Kalman Filter** | 1-2 | 把 **C2** 的 log β random walk 換成線性高斯狀態空間，用 KF 解析更新對照 MCMC |

**補各專案限制的**（來自 README 的「限制與誠實的部分」）：

| 想做什麼 | 相關專案 | 為什麼（哪個限制）|
|---|---|---|
| 連續 Bernoulli 似然 | S1 | 限制 2：Bernoulli 對 [0,1] 連續像素是未正規化的近似 |
| 全解析度胸片重跑 | S1, A3 | 限制 4：28×28 抹掉大部分病理特徵 |
| 每個 β 都跑多 seed | S1 | 限制 7：β=16 的優勢只有 2.2σ，單 seed 掃描 |
| random walk 加漂移項 | C2 | 樣本外 50%/80% 帶覆蓋不足，外推無漂移 |
| 橢球變形相位曲線建模 | B2 | M2 沒建模，那是 EB 的第三個證據 |
| 星震密度先驗 | B1 | a/R* 偏高，簡併沒被外部資訊打斷 |
| | | |
