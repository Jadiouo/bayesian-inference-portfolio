---
title: 貝葉斯推論 · 跨領域實作訓練計劃
subtitle: 五個領域,六件作品集,一套統一的數學
date: 2026-07-28
version: 2.0
tags:
  - 機器學習
  - 貝葉斯推論
  - 實作計劃
  - 作品集
aliases:
  - Bayesian Portfolio Plan
status: 未開始
---

# 貝葉斯推論 · 跨領域實作訓練計劃

> [!abstract] v2.0 更新說明
> 上一版以 IMU / EuRoC 為主軸。既然那條線你已經走過,這一版**整個換掉領域**,選了五個**貝葉斯真正是業界標準做法**的領域。
>
> **選領域的三個標準**:
> 1. **貝葉斯不是「一種選擇」,而是該領域的主流工具** —— 這樣你的作品才有說服力
> 2. **資料公開、無需申請、可重現** —— 別人能驗證你的結果
> 3. **五個領域互不重疊** —— 履歷上看起來是「一套方法論」,不是「一個技巧重複五次」

> [!tip] 核心敘事(這是你履歷的主軸,先想清楚)
> 你不是在做「五個不相干的專案」。你在展示**一件事**:
>
> > **「我能量化模型的不確定性,並把它轉換成可執行的決策。」**
>
> 這句話在醫療是「該不該轉診」,在天文是「這是不是行星」,在商業是「該不該上線」。
> **同一套數學,五個領域**。這比五個 CNN 分類器有價值得多。

---

## 目錄

- [[#0 · 環境準備]]
- [[#1 · 五個領域的地圖]]
- [[#2 · 領域 A · 醫療決策與診斷不確定性]]
- [[#3 · 領域 B · 天文物理:系外行星偵測]]
- [[#4 · 領域 C · 流行病學與階層模型]]
- [[#5 · 領域 D · 商業決策:貝葉斯 A/B 測試]]
- [[#6 · 領域 E · 科學實驗設計:貝葉斯最優化]]
- [[#7 · 理論覆蓋檢核表]]
- [[#8 · 怎麼把這些放進履歷]]
- [[#9 · 常見陷阱]]
- [[#10 · 進度追蹤]]

---

## 0 · 環境準備

### 0.1 建議環境

> [!tip] 先用 Colab 跑通,再考慮本機
> https://colab.research.google.com —— 零安裝,`numpy` / `torch` 都內建。

```bash
# 核心
pip install numpy scipy matplotlib pandas scikit-learn

# 貝葉斯主力(這一版大量使用)
pip install pymc arviz              # MCMC / 階層模型 / 模型比較
pip install torch                   # BNN / MC Dropout

# 領域專用
pip install lifelines scikit-survival   # 領域 A(生存分析)
pip install lightkurve batman-package   # 領域 B(天文)
pip install ultranest                   # 領域 B(nested sampling / 貝氏因子)
pip install medmnist                    # 領域 A(醫學影像,輕量)
pip install scikit-optimize             # 領域 E(貝葉斯最優化)
```

> [!warning] 兩個安裝陷阱
> - `pymc` 在 Windows 上偶爾會有 C 編譯器問題 → **強烈建議用 Colab 或 WSL**
> - `lightkurve` 第一次下載光曲線需要網路,Colab 上會快得多

### 0.2 熱身測試(10 分鐘)

在動任何專案前,確認你能畫出「一座山」而且會用 PyMC:

```python
import numpy as np, matplotlib.pyplot as plt
import pymc as pm, arviz as az

# 場景:一個診斷檢測,20 人受檢,3 人陽性。真陽性率是多少?
n, k = 20, 3

with pm.Model() as m:
    theta = pm.Beta("theta", alpha=1, beta=1)      # 先驗
    pm.Binomial("obs", n=n, p=theta, observed=k)   # 似然
    idata = pm.sample(2000, chains=4, random_seed=42)

az.plot_posterior(idata, hdi_prob=0.95)
plt.show()
print(az.summary(idata))          # 看 r_hat 是否 < 1.01
```

> [!success] 通關標準
> 1. ✅ 螢幕上出現一座偏左的山,峰值大約在 0.15
> 2. ✅ `r_hat` 全部 < 1.01(收斂了)
> 3. ✅ 95% HDI 大約是 `[0.04, 0.33]` —— 注意它有多寬!**20 個樣本能告訴你的事情很有限**
>
> 這張圖就是主題三第三卡的「可信區間」。**你可以合法地說「真陽性率有 95% 機率落在這個區間」。**

---

## 1 · 五個領域的地圖

| 領域 | 為什麼貝葉斯在這裡是主流? | 主要練到的主題 | 履歷關鍵字 |
|---|---|---|---|
| **A · 醫療決策** | 診斷本質是後驗推論;誤診代價不對稱;模型必須知道自己不知道 | 一、三、六、七 | 不確定性量化 · 決策理論 · 校準 |
| **B · 天文物理** | MCMC 和 nested sampling **就是**系外行星論文的標準工具;貝氏因子用來判定「這是不是行星」 | 四、五、七 | MCMC · 模型比較 · 貝氏因子 |
| **C · 流行病學** | 階層模型是公衛統計的核心;小樣本借力(partial pooling)是它的殺手鐧 | 一、二、四 | 階層貝葉斯 · 部分匯聚 · 先驗設計 |
| **D · 商業 A/B 測試** | 業界已大量從 p-value 轉向貝葉斯;能直接回答「B 比 A 好的機率」 | 三、七 | 期望損失 · 序貫決策 · 業務影響 |
| **E · 實驗設計** | 貝葉斯最優化是 AutoML / 材料科學 / 藥物篩選的標準工具 | 二、七 | GP · Acquisition Function · 探索利用 |

> [!info] 建議路徑
> **想快速做出第一件作品** → 領域 A 專案 A1(醫療決策)。它最短、最有故事性,而且直接呼應課堂上的檢測悖論。
>
> **想做出最「硬」的作品** → 領域 B(天文)。那是真正的科學研究流程,而且結果可以和已發表的論文數值比對。
>
> **想最貼近業界職缺** → 領域 D(A/B 測試)+ 領域 E(貝葉斯最優化)。

---

## 2 · 領域 A · 醫療決策與診斷不確定性

> [!quote] 這個領域的核心提問
> 「模型說『90% 是惡性』。**那我該開刀嗎?**」
>
> 這個問題把主題一(後驗)、主題六(不確定性分解)、主題七(決策)全部串在一起。

### 2.1 資料集

| # | 名稱 | 內容 | 取得 |
|---|---|---|---|
| **A-D1** | **Heart Disease (Cleveland)** | 303 筆,13 個臨床特徵,二元診斷 | https://archive.ics.uci.edu/dataset/45/heart+disease |
| **A-D2** | **Pima Indians Diabetes** | 768 筆,8 個特徵,糖尿病診斷 | https://www.openml.org/d/37 或 `sklearn.datasets.fetch_openml('diabetes', version=1)` |
| **A-D3** | **Breast Cancer Wisconsin** | 569 筆,30 個特徵 | `sklearn.datasets.load_breast_cancer()` |
| **A-D4** | **lifelines 內建生存資料** | Rossi / Stanford heart transplant / 等 | `from lifelines.datasets import load_rossi` |
| **A-D5** | **GBSG · METABRIC** | 乳癌生存資料(DeepSurv 論文使用的標準版本) | https://github.com/jaredleekatzman/DeepSurv/tree/master/experiments/data |
| **A-D6** | **MedMNIST v2** | 12 個醫學影像資料集,全部 28×28,**輕量到能在筆電跑** | `pip install medmnist`;https://medmnist.com |
| **A-D7** | **NIH ChestX-ray14** | 112,120 張胸腔 X 光,14 種病徵 | https://nihcc.app.box.com/v/ChestXray-NIHCC(公開,無需申請) |

> [!warning] 關於醫療資料的取得門檻
> **MIMIC-IV、MIMIC-CXR、VinDr-CXR 需要 PhysioNet 認證**(要完成 CITI 人類受試者訓練,通常要幾小時到幾天)。
> **這份計劃刻意避開它們** —— 上表全部免申請。
>
> 如果你之後想深入醫療 AI,做 PhysioNet 認證是值得的,但別讓它擋住你現在開始。

---

### 專案 A1 · 診斷決策系統:從後驗到「該不該治療」⭐ 建議第一個做

> [!question] 核心問題
> 「模型輸出一個機率。**把它變成行動的那一步,數學長什麼樣?**」

**資料**:A-D1(Heart Disease)或 A-D2(Diabetes)

#### 實作步驟

1. **貝葉斯邏輯迴歸(用 PyMC)**
   ```python
   with pm.Model() as m:
       beta = pm.Normal("beta", 0, 2.5, shape=n_features)   # 弱資訊先驗
       alpha = pm.Normal("alpha", 0, 5)
       p = pm.math.sigmoid(alpha + pm.math.dot(X, beta))
       pm.Bernoulli("y", p=p, observed=y)
       idata = pm.sample(2000, chains=4)
   ```
   > 為什麼先驗用 $\mathcal{N}(0, 2.5)$?這是邏輯迴歸的標準弱資訊先驗(主題二第一卡)。
   > **順便做敏感度分析**:換成 $\mathcal{N}(0,10)$ 和 $\mathcal{N}(0,1)$,看結論穩不穩。

2. **後驗預測分佈,不是 plug-in**(主題三第二卡)
   - 對每個測試病人,用**全部後驗樣本**各預測一次,再平均
   - 對照組:只用後驗均值做一次預測
   - **比較兩者的預測區間寬度**

3. **建損失矩陣,算最優門檻**(主題七第一卡)

   | | 真的有病 | 沒病 |
   |---|---|---|
   | **治療** | 0 | $C_{FP}$ |
   | **不治療** | $C_{FN}$ | 0 |

   掃過 $C_{FN}/C_{FP} \in \{1, 5, 20, 100\}$,對每個比值算最優門檻 $p^* = \frac{C_{FP}}{C_{FP}+C_{FN}}$

4. **加入棄權選項**(主題七第一卡的核心)
   - 第三個行動:「轉診 / 再檢查」,固定成本 $C_{\text{reject}}$
   - 畫出**三條期望損失曲線**,標出棄權區
   - **報告在測試集上,有多少比例的病人落入棄權區**

5. **校準分析(Calibration)** —— 這一步最能展現專業度
   - 畫 **reliability diagram**:把預測機率分成 10 個 bin,看每個 bin 的實際發生率
   - 算 **ECE(Expected Calibration Error)**
   - 對照:普通邏輯迴歸 vs 貝葉斯邏輯迴歸,誰的校準比較好?

#### 必須看見的現象

> [!success] 通關標準
> 1. ✅ 損失比 100:1 時,最優門檻掉到 **約 0.01** —— 和預設的 0.5 差兩個數量級
> 2. ✅ 用 0.5 當門檻時的期望損失,**明顯高於**用最優門檻
> 3. ✅ 棄權區在圖上清楚可見,而且 $C_{\text{reject}}$ 越小區間越寬
> 4. ✅ Reliability diagram 上,理想是貼在對角線上

#### 費曼自我檢驗
- [ ] 我能對非技術背景的人解釋「為什麼門檻不是 0.5」嗎?
- [ ] 後驗預測分佈和 plug-in 預測,差在哪個具體數字上?
- [ ] 校準差的模型,即使 AUC 很高,為什麼在決策上仍然危險?

---

### 專案 A2 · 貝葉斯生存分析:不只預測會不會,還要預測何時

> [!question] 核心問題
> 「有些病人在追蹤期結束時**還活著**——這筆資料不是『沒事件』,是『我還不知道』。**這種「刪失(censoring)」怎麼進入似然?**」

**資料**:A-D4(lifelines 內建)入門 → A-D5(GBSG / METABRIC)進階

#### 實作步驟

1. **先理解刪失的似然**(這是本專案的靈魂)
   - 觀察到事件($t_i$ 時死亡):貢獻 $f(t_i)$ —— 機率密度
   - **右刪失**($t_i$ 時仍存活):貢獻 $S(t_i) = 1 - F(t_i)$ —— **存活函數,不是密度**
   $$\mathcal{L} = \prod_{i: \text{事件}} f(t_i) \prod_{i: \text{刪失}} S(t_i)$$
   > 🔑 **刪失資料不是遺失資料**。它攜帶真實資訊:「至少活到了 $t_i$」。

2. **手刻參數化貝葉斯生存模型**
   - Weibull 分佈:$S(t) = \exp(-(t/\lambda)^k)$
   - 用 PyMC 的 `pm.Weibull` 搭配 `pm.Potential` 處理刪失,或用 `pm.Censored`

3. **對照:頻率派 Cox 模型**
   - `lifelines.CoxPHFitter` 給你點估計 + 信賴區間
   - **你的貝葉斯版本給你完整後驗** —— 對比兩者的區間解讀差異(主題三第三卡!)

4. **個體化存活曲線 + 不確定性帶**
   - 對一個特定病人,畫出他的存活曲線
   - **加上後驗預測的信賴帶** —— 這是貝葉斯的獨門好處

5. **貝葉斯模型比較**(主題七第三卡)
   - 比較 Weibull vs Log-Normal vs Exponential 基準風險
   - 用 **WAIC / LOO-CV**(`az.compare(...)`)
   - > 💡 為什麼不用貝氏因子?因為它對先驗太敏感(Lindley 悖論)。**WAIC/LOO 是實務上的主流選擇**。

#### 必須看見的現象

> [!success] 通關標準
> 1. ✅ 忽略刪失(把刪失當成事件)→ **存活時間被系統性低估**。你要做出這個對照
> 2. ✅ 貝葉斯的個體存活曲線帶有不確定性帶,而 Cox 只給一條線
> 3. ✅ `az.compare` 能明確排序三個模型,而且你能解釋 WAIC 的懲罰項在做什麼

---

### 專案 A3 · 醫學影像的不確定性與 OOD 偵測

> [!question] 核心問題
> 「模型在訓練分佈內表現很好。**餵它一張從沒見過的影像,它知道自己在瞎猜嗎?**」

**資料**:A-D6(MedMNIST,**強烈建議從這個開始**)

MedMNIST 的優勢:12 個資料集、全部 28×28、每個幾十 MB、**可以在筆電上跑完整實驗**。
- `PathMNIST`(病理切片)· `DermaMNIST`(皮膚病灶)· `PneumoniaMNIST`(肺炎胸片)· `BloodMNIST`(血球)· `OCTMNIST`(視網膜)

#### 實作步驟

1. **訓練一個帶 Dropout 的 CNN**,在 `PneumoniaMNIST` 上

2. **MC Dropout 分解不確定性**(主題六第三卡)
   ```python
   model.train()           # ← 關鍵:測試時保持 Dropout 開啟
   probs = torch.stack([model(x).softmax(-1) for _ in range(50)])  # (50, N, C)

   mean_p    = probs.mean(0)
   total     = -(mean_p * mean_p.log()).sum(-1)                 # 總熵
   aleatoric = -(probs * probs.log()).sum(-1).mean(0)           # 各次熵的平均
   epistemic = total - aleatoric                                # 互資訊
   ```
   > 🔑 分類問題的分解用**熵**而不是變異數。`epistemic = 總熵 - 平均熵` 這個量在資訊理論裡就是**互資訊**——「知道 $w$ 能減少多少對 $y$ 的不確定性」。

3. **三組 OOD 實驗**(難度遞增)
   | 訓練 | 測試 OOD | 難度 |
   |---|---|---|
   | PneumoniaMNIST | **FashionMNIST** | 簡單(完全不同的東西) |
   | PneumoniaMNIST | **DermaMNIST** | 中等(都是醫學影像,但不同器官) |
   | PathMNIST(9 類) | **留出其中 2 類不訓練** | 困難(同一分佈的未見類別) |

4. **量化 OOD 偵測效能**
   - 用 epistemic uncertainty 當作 OOD 分數
   - 算 **AUROC**(區分 in-distribution vs OOD)
   - 對照三種方法:**Softmax 最大值** vs **MC Dropout epistemic** vs **Deep Ensemble**

5. **接上決策層**(把 A1 和 A3 縫起來)
   - epistemic 超過門檻 → 觸發「轉人工判讀」
   - 畫 **risk-coverage 曲線**:「我放棄 X% 的樣本,剩下的準確率能到多少?」

#### 必須看見的現象

> [!success] 通關標準
> 1. ✅ OOD 樣本的 epistemic 直方圖**明顯右移**
> 2. ✅ 難度遞增的三組實驗,AUROC 依序下降 —— **困難的那組可能接近 0.6,這是誠實的結果**
> 3. ✅ Risk-coverage 曲線上,放棄 20% 最不確定的樣本,準確率明顯提升
> 4. ✅ Deep Ensemble 通常比 MC Dropout 好 —— **如果你的結果相反,要去查為什麼**

> [!warning] 誠實面對的部分
> 近年有不少論文指出 **MC Dropout 的 OOD 偵測效能常常被高估**。
> **如果你的實驗做出「效果沒有想像中好」的結論,那是好事** —— 誠實報告負面結果,比硬做出漂亮數字更有專業價值。

---

## 3 · 領域 B · 天文物理:系外行星偵測

> [!quote] 為什麼這個領域值得做
> **在系外行星研究裡,MCMC 和 nested sampling 不是「一種方法」,它們就是標準流程。**
> 每一篇行星發現論文都在做貝葉斯參數估計 + 貝氏因子模型比較。
>
> 這是唯一一個「你的作品流程 = 真實科學研究流程」的領域。

### 3.1 資料集

| # | 名稱 | 取得 |
|---|---|---|
| **B-D1** | **Kepler / K2 / TESS 光曲線** | `lightkurve` 套件直接抓:`lightkurve.search_lightcurve("Kepler-10")` |
| **B-D2** | **NASA Exoplanet Archive** | https://exoplanetarchive.ipac.caltech.edu —— 已確認行星的參數(拿來對答案!) |
| **B-D3** | **Kepler Objects of Interest (KOI)** | 同上,含 CONFIRMED / FALSE POSITIVE 標籤 |

> [!tip] 建議的第一個目標:**Kepler-10b**
> - 軌道週期約 20 小時 —— **極短**,所以 4 年的資料裡有幾千次凌日
> - 訊號清楚,是教科書級的範例
> - 已發表的參數可以拿來驗證你的後驗

---

### 專案 B1 · 凌日光曲線的貝葉斯參數估計

> [!question] 核心問題
> 「一顆行星經過恆星前面,亮度掉了 0.02%。**從這個微小的凹陷,我能推論出行星多大、軌道多長嗎?而且我對這些數字有多確定?**」

#### 實作步驟

1. **下載並清理光曲線**
   ```python
   import lightkurve as lk
   lc = lk.search_lightcurve("Kepler-10", author="Kepler", cadence="long").download_all()
   lc = lc.stitch().remove_nans().remove_outliers().flatten(window_length=901)
   ```

2. **相位摺疊**(phase folding)
   - 用已知週期把幾千次凌日疊在一起 → 訊噪比大幅提升
   - **這一步本身就是一種「資料融合」** —— 精神上和序貫更新一致

3. **建立物理模型**
   - 用 `batman` 套件生成凌日模型
   - 待估參數:週期 $P$、凌日中心時刻 $t_0$、行星/恆星半徑比 $R_p/R_*$、軌道傾角 $i$、臨邊昏暗係數

4. **先驗設計**(主題二 —— 這裡是真的要動腦的地方)
   | 參數 | 建議先驗 | 為什麼 |
   |---|---|---|
   | $P$ | 窄的常態(以初步偵測值為中心) | 週期已由 BLS 演算法粗估 |
   | $R_p/R_*$ | **log-uniform** 或 half-normal | **它是正數!**(主題二第一卡的教訓) |
   | $i$ | $\cos i \sim \text{Uniform}$ | **幾何上均勻,不是角度均勻** ← 這就是參數化不變性 |
   | 臨邊昏暗 | Kipping (2013) 的三角參數化 | 物理上有效範圍的重參數化 |

   > 🔑 **$\cos i$ 均勻而不是 $i$ 均勻** —— 這是主題二第一卡「均勻不等於無資訊」的真實應用場景。

5. **MCMC 採樣**
   - 用 `emcee`(天文界標準)或 `PyMC`
   - **完整診斷**:trace plot、$\hat R$、autocorrelation time、ESS

6. **和已發表值比對**
   - 從 NASA Exoplanet Archive 查 Kepler-10b 的公認參數
   - **你的後驗有沒有涵蓋它?** 這是你的作品最有力的驗證

#### 必須看見的現象

> [!success] 通關標準
> 1. ✅ 相位摺疊後的凌日凹陷清晰可見
> 2. ✅ 後驗的 corner plot 上,某些參數**強烈相關**(例如 $R_p/R_*$ 和 $i$)—— 這正是 Mean Field VI 會失敗的地方!
> 3. ✅ 你估的 $R_p/R_*$ 的 95% HDI **涵蓋已發表值**
> 4. ✅ 後驗預測檢查:從後驗採樣的模型曲線束,能包住觀測資料

---

### 專案 B2 · 貝氏因子:這到底是不是行星?

> [!question] 核心問題
> 「光曲線上有一個凹陷。**是行星凌日?還是食雙星?還是儀器雜訊?**」
>
> **這正是主題七第三卡的貝葉斯模型選擇 —— 而且在天文界是真的這樣做的。**

#### 實作步驟

1. **建立三個競爭模型**
   | 模型 | 說明 | 參數數 |
   |---|---|---|
   | $M_0$ | **純雜訊**(沒有凌日) | 少 |
   | $M_1$ | **行星凌日**(對稱、平底) | 中 |
   | $M_2$ | **食雙星**(V 字形、有次食) | 多 |

2. **用 nested sampling 算邊際似然**
   ```python
   import ultranest
   sampler = ultranest.ReactiveNestedSampler(param_names, loglike, prior_transform)
   result = sampler.run()
   logZ = result['logz']          # ← 這就是 log p(D|M)
   ```
   > 🔑 **為什麼用 nested sampling 而不是 MCMC?**
   > MCMC 用比值消掉了 $p(D)$(主題四第三卡)—— 這正是它算不出邊際似然的原因。
   > **Nested sampling 是專門設計來直接計算 $\log Z = \log p(D\mid M)$ 的**。

3. **算貝氏因子並解讀**
   $$\log B_{10} = \log Z_1 - \log Z_0$$
   對照 Jeffreys 尺度($\log_{10} B > 2$ 為決定性證據)

4. **拿真的 FALSE POSITIVE 來測試** ⭐ 這一步最能體現嚴謹
   - 從 KOI 表裡挑幾個標記為 **FALSE POSITIVE** 的目標
   - **你的貝氏因子應該不支持行星模型** —— 如果支持了,去搞清楚為什麼

5. **先驗敏感度分析**(誠實面對 Lindley 悖論)
   - 把 $R_p/R_*$ 的先驗放寬 10 倍,重算貝氏因子
   - **記錄結論變了多少** —— 這是主題七第三卡警告過的弱點

#### 必須看見的現象

> [!success] 通關標準
> 1. ✅ 已確認的行星(如 Kepler-10b):$\log_{10}B_{10}$ **遠大於 2**
> 2. ✅ FALSE POSITIVE 目標:貝氏因子**不支持**行星模型
> 3. ✅ 先驗放寬 10 倍後,貝氏因子**明顯改變** —— 你親眼看到了 Lindley 悖論
> 4. ✅ 複雜模型($M_2$)即使擬合更好,**邊際似然仍可能較低** ← 奧卡姆剃刀現場

---

## 4 · 領域 C · 流行病學與階層模型

> [!quote] 為什麼這個領域值得做
> **階層貝葉斯(hierarchical Bayes)是這門課沒教到、但威力最大的一招。**
> 它回答一個很實際的問題:「我有 50 個小樣本的群組,每個都不夠做統計。**能不能讓它們互相借力?**」

### 4.1 資料集

| # | 名稱 | 內容 | 取得 |
|---|---|---|---|
| **C-D1** | **Radon** | 美國 919 棟房屋的氡氣濃度,分佈在 85 個郡 —— **階層模型的教科書範例** | PyMC 範例庫:https://www.pymc.io/projects/examples |
| **C-D2** | **Eight Schools** | 8 所學校的教學成效,只有 8 列 —— 最小的階層模型 | `az.load_arviz_data("eight_schools")` |
| **C-D3** | **Our World in Data · COVID** | 各國每日確診/死亡/疫苗,可依國家分層 | https://github.com/owid/covid-19-data |
| **C-D4** | **WHO Global Health Observatory** | 各國健康指標,適合跨國階層建模 | https://www.who.int/data/gho |
| **C-D5** | **Efron–Morris 棒球打擊率** | 18 位球員前 45 打數 —— 收縮估計的原始論文資料 | 只有 18 列,可手動輸入 |

---

### 專案 C1 · 階層模型與「部分匯聚」的魔法 ⭐ 概念收穫最大

> [!question] 核心問題
> 「某個郡只量了 2 棟房子,平均值很不可靠。**我該相信它自己的平均,還是全國平均?**」
>
> **貝葉斯的答案:兩個都不要,要一個「按可靠度加權」的中間值。**

**資料**:C-D1(Radon)。想更快看到現象就先用 C-D2 或 C-D5。

#### 實作步驟

1. **先做兩個極端(建立對照)**
   | 模型 | 做法 | 問題 |
   |---|---|---|
   | **完全不匯聚**(no pooling) | 每個郡各算各的平均 | 小樣本的郡估計極不穩定 |
   | **完全匯聚**(complete pooling) | 全部混在一起算一個平均 | 完全忽略郡的差異 |

2. **階層模型(部分匯聚)**
   ```python
   with pm.Model() as hierarchical:
       # 超先驗:郡效應的母體分佈
       mu_a    = pm.Normal("mu_a", 0, 5)
       sigma_a = pm.HalfNormal("sigma_a", 5)      # ← 正數!用 HalfNormal

       # 每個郡的截距,從母體分佈抽出
       a = pm.Normal("a", mu_a, sigma_a, shape=n_counties)

       b     = pm.Normal("b", 0, 5)
       sigma = pm.HalfNormal("sigma", 5)
       mu    = a[county_idx] + b * floor
       pm.Normal("y", mu, sigma, observed=log_radon)
   ```

3. **畫出「收縮圖」(shrinkage plot)** —— 這是本專案的靈魂
   - 橫軸:每個郡的樣本數
   - 縱軸:階層估計 vs 各郡自己的平均
   - **樣本數少的郡,估計被大幅拉向全國平均;樣本數多的郡,幾乎不動**

4. **驗證預測效能**
   - 留出測試集,比較三個模型的預測誤差
   - **階層模型應該勝出,尤其在小樣本的郡**

#### 必須看見的現象

> [!success] 通關標準
> 1. ✅ 收縮圖上,「拉力」隨樣本數增加而遞減 —— **這條曲線就是部分匯聚的視覺證明**
> 2. ✅ 階層模型的測試誤差 < 兩個極端模型
> 3. ✅ $\sigma_a$ 的後驗告訴你「郡與郡之間差異有多大」—— **這是完全匯聚模型無法回答的問題**

> [!note] 這其實是你學過的東西
> **部分匯聚 = 精確度加權平均**(主題二第二卡)。
> 郡自己的資料是「似然」,母體分佈是「先驗」,估計就是兩者按精確度加權 —— 只是這次**先驗本身也是從資料學出來的**(這叫 empirical Bayes / 階層貝葉斯)。

---

### 專案 C2 · 疾病傳播模型的貝葉斯參數估計

> [!question] 核心問題
> 「從觀察到的每日確診數,能不能反推出傳染率 $\beta$ 和有效再生數 $R_t$?**而且給出誠實的不確定性?**」

**資料**:C-D3(Our World in Data COVID)

> [!warning] 定位說明
> 這是一個**技術練習**,目的是練階層模型與狀態空間推論。
> 真實的流行病學建模牽涉大量領域知識(通報延遲、檢驗量能、行為改變),**不要把練習結果當成公衛結論**。
> 在你的作品說明裡誠實標註這一點,反而會顯得更專業。

#### 實作步驟

1. **選一個國家、一段時間**(建議選資料品質好、疫情曲線清楚的區間)

2. **建 SIR 模型的貝葉斯版本**
   - 待估參數:傳染率 $\beta$、恢復率 $\gamma$、初始感染人數 $I_0$、通報率
   - 先驗:$\gamma$ 用文獻的資訊先驗(平均病程約 1/10 天),$\beta$ 用弱資訊先驗

3. **時變 $R_t$ 的估計** —— 這裡是重點
   - 讓 $\beta_t$ 隨時間變化,加上 **random walk 先驗**:$\log\beta_t \sim \mathcal{N}(\log\beta_{t-1}, \sigma_\beta)$
   - > 🔑 **這是主題一第二卡的序貫更新 + 主題二的先驗設計的組合** —— random walk 先驗就是在說「我相信 $\beta$ 會變,但不會突然跳」

4. **後驗預測檢查**
   - 從後驗採樣,模擬未來 14 天
   - 畫出預測帶,和實際資料比對
   - **重點不是預測準,是「預測帶有沒有包住真實值」** —— 這是校準

#### 必須看見的現象

> [!success] 通關標準
> 1. ✅ $R_t$ 的後驗有清楚的不確定性帶,而且在政策改變的時點附近有變化
> 2. ✅ 資料稀疏的時期,不確定性帶**明顯變寬** —— Epistemic 的直接體現
> 3. ✅ 後驗預測帶大約有 95% 的時間包住真實觀測

---

## 5 · 領域 D · 商業決策:貝葉斯 A/B 測試

> [!quote] 為什麼這個領域值得做
> **業界正在大規模從 p-value 轉向貝葉斯 A/B 測試。**
> 原因很直接:老闆問的是「B 比 A 好的機率是多少」,而 p-value **在定義上無法回答這個問題**。
>
> 這是五個領域裡**最容易在面試中講出價值**的一個。

### 5.1 資料集

| # | 名稱 | 內容 | 取得 |
|---|---|---|---|
| **D-D1** | **Criteo Uplift Modeling** | 2500 萬筆廣告曝光,含對照組 | https://ailab.criteo.com/criteo-uplift-prediction-dataset/ |
| **D-D2** | **Online Shoppers Purchasing Intention** | 12,330 筆線上購物 session | https://archive.ics.uci.edu/dataset/468/online+shoppers+purchasing+intention+dataset |
| **D-D3** | **Bank Marketing** | 45,211 筆行銷活動結果 | https://archive.ics.uci.edu/dataset/222/bank+marketing |
| **D-D4** | **模擬資料** | 自己生成 —— **A/B 測試的方法論研究,模擬反而更好** | 自己寫 |

> [!tip] 這個領域建議「模擬 + 真實」並用
> 方法論的驗證(型一錯誤率、偷看資料的影響)**必須用模擬**,因為你要知道真相。
> 真實資料則用來展示「在髒資料上也能跑」。

---

### 專案 D1 · 貝葉斯 A/B 測試框架

> [!question] 核心問題
> 「A 版轉換率 5.2%,B 版 5.8%。**B 真的比較好嗎?我該不該上線?**」

#### 實作步驟

1. **Beta-Binomial 共軛更新**(主題二第二卡的直接應用)
   - 兩組各自的後驗:$\theta_A \sim \text{Beta}(1 + k_A,\; 1 + n_A - k_A)$
   - **不需要 MCMC,封閉解就夠** —— 這也是它在業界受歡迎的原因(即時計算)

2. **回答老闆真正的問題**
   ```python
   # 蒙地卡羅:從兩個後驗各採樣
   sa = np.random.beta(1+kA, 1+nA-kA, 200_000)
   sb = np.random.beta(1+kB, 1+nB-kB, 200_000)

   print("P(B 優於 A) =", (sb > sa).mean())
   print("提升幅度的 95% 區間:", np.percentile(sb/sa - 1, [2.5, 97.5]))
   print("期望損失(若選 B 但其實 A 較好) =", np.maximum(sa - sb, 0).mean())
   ```
   > 🔑 **最後那個「期望損失」就是主題七第一卡的決策規則** —— 業界叫它 **expected loss / risk**,常見的上線規則是「期望損失低於某個閾值就上線」。

3. **序貫測試:偷看資料的影響** ⭐ 這是最有說服力的實驗
   - **模擬 1000 次 A/A 測試**(兩組其實一樣)
   - **每天偷看一次,只要 $p < 0.05$ 就宣布勝利**
   - **記錄假陽性率** —— 會遠高於 5%(通常 20–30%)
   - **同樣的偷看策略,用貝葉斯期望損失規則,假陽性率是多少?**

4. **加入決策成本**
   - 上線 B 的成本(工程時間、風險)
   - 維持 A 的機會成本
   - **算出「值得上線」的最小提升幅度**

#### 必須看見的現象

> [!success] 通關標準
> 1. ✅ 你能輸出「B 優於 A 的機率 = 87%」這種老闆聽得懂的話
> 2. ✅ 頻率派偷看實驗的假陽性率**遠超過 5%** —— 這張圖非常有衝擊力
> 3. ✅ 貝葉斯決策規則在同樣的偷看策略下表現較穩健
> 4. ✅ 你能說明貝葉斯 A/B **不是萬靈丹** —— 它的結論仍依賴先驗和損失函數的設定

> [!note] 誠實的部分
> 貝葉斯 A/B「可以隨時偷看」這個說法**常被過度宣傳**。
> 準確的說法是:**貝葉斯的量(後驗機率、期望損失)在偷看下的解讀不會失效,但如果你用「後驗機率 > 95% 就停」當停止規則,長期錯誤率仍然會受影響。**
> 能講清楚這個細節,會讓你在面試中明顯高出一截。

---

## 6 · 領域 E · 科學實驗設計:貝葉斯最優化

> [!quote] 為什麼這個領域值得做
> 貝葉斯最優化是 **AutoML、材料科學、藥物篩選、製程調校**的標準工具。
> 它也是主題七第二卡的完整落地。

### 6.1 資料集 / 目標函數

| # | 名稱 | 用途 | 取得 |
|---|---|---|---|
| **E-D1** | **標準測試函數** | Branin / Hartmann6 / Ackley —— 有已知最優解,可驗證 | `scipy` 或手寫 |
| **E-D2** | **UCI Concrete Compressive Strength** | 混凝土配方 → 強度。**把它當成「材料配方最優化」** | https://archive.ics.uci.edu/dataset/165/concrete+compressive+strength |
| **E-D3** | **Superconductivity** | 21,263 筆材料 → 臨界溫度 | https://archive.ics.uci.edu/dataset/464/superconductivty+data |
| **E-D4** | **HPOBench / LCBench** | 真實的超參數最優化基準(有預先算好的結果,可離線模擬) | https://github.com/automl/HPOBench |

---

### 專案 E1 · 從零實作貝葉斯最優化

> [!question] 核心問題
> 「每次實驗要 8 小時。**我只有 30 次機會。第 11 次該做在哪裡?**」

#### 實作步驟

1. **手刻 GP 迴歸**(不用套件,理解機制)
   - 核函數:先 RBF,再試 Matérn 5/2
   - 後驗:$\mu(x) = k_*^\top (K+\sigma^2 I)^{-1} y$,$\sigma^2(x) = k_{**} - k_*^\top(K+\sigma^2 I)^{-1}k_*$
   - > 🔑 **這兩條式子就是高斯共軛在函數空間的版本**(主題二第二卡)

2. **實作三種 acquisition function**
   | 名稱 | 公式 |
   |---|---|
   | **UCB** | $\mu(x) + \kappa\sigma(x)$ |
   | **EI** | $(\mu - f^+)\Phi(Z) + \sigma\phi(Z),\; Z = \frac{\mu - f^+}{\sigma}$ |
   | **Thompson** | 從 GP 後驗採樣一個函數,取它的最大值位置 |

3. **在標準測試函數上比較**(E-D1)
   - 對照組:**隨機搜尋**
   - 畫「試驗次數 vs 目前最佳值」的曲線,**跑 30 次不同隨機種子取平均 ± 標準差**
   - > ⚠️ 只跑一次的比較沒有意義。**多次重複 + 誤差帶**是這個專案的專業度所在。

4. **$\kappa$ 的敏感度實驗**(主題七第二卡)
   - 掃 $\kappa \in \{0, 0.5, 2, 5, 20\}$
   - **$\kappa=0$ 應該卡在局部最優;$\kappa=20$ 應該退化成接近隨機搜尋**

5. **真實應用:材料配方最優化**(E-D2)
   - 訓練一個代理模型當作「真實世界」(或直接在資料集上做最近鄰查詢)
   - 用貝葉斯最優化尋找「最高抗壓強度的配方」
   - **對照隨機搜尋,計算「省下多少次實驗」**

6. **維度詛咒實驗** ⭐ 這一步展現判斷力
   - 在 2 / 5 / 10 / 20 / 50 維的 Ackley 函數上重複比較
   - **找出「貝葉斯最優化不再勝過隨機搜尋」的維度臨界點**
   - 這就是主題七第二卡的失敗模式,你親手量出來

#### 必須看見的現象

> [!success] 通關標準
> 1. ✅ 低維(< 10)時,貝葉斯最優化明顯勝過隨機搜尋
> 2. ✅ $\kappa = 0$ 時卡在局部最優的軌跡清晰可見
> 3. ✅ 高維時兩者收斂到接近 —— **你量出了自己的臨界維度**
> 4. ✅ 換核函數(RBF → Matérn)會改變結果 —— **核函數就是先驗**(主題二)

---

## 7 · 理論覆蓋檢核表

> [!info] 確認七個主題都有被練到

| 主題 | 專案 A1 | A2 | A3 | B1 | B2 | C1 | C2 | D1 | E1 | **S1** |
|---|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
| **一 · 貝葉斯定理 / 序貫更新** | ✅ | ✅ | | ✅ | | ✅ | ✅ | ✅ | ✅ | |
| **二 · 先驗設計** | ✅ | ✅ | | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | |
| **三 · 後驗解讀 / 可信區間** | ✅ | ✅ | | ✅ | | ✅ | ✅ | ✅ | | |
| **四 · 近似推論(MCMC)** | ✅ | ✅ | | ✅ | ✅ | ✅ | ✅ | | | |
| **五 · 變分推論 / ELBO** | | | ✅ | | | | | | | **✅** |
| **六 · 不確定性分解** | ✅ | | ✅ | | | | ✅ | | ✅ | ✅ |
| **七 · 決策 / 最優化 / 模型選擇** | ✅ | ✅ | ✅ | | ✅ | | | ✅ | ✅ | |

> [!success] 主題五(VI / ELBO)已由 S1 補上 ✅
> **S1 · 醫學影像的 VAE 異常偵測**([`S1_vae_anomaly/`](S1_vae_anomaly/README.md))
> - 資料:MedMNIST(A-D6)的 PneumoniaMNIST,只用 1214 張 normal 訓練
> - 在**正常**樣本上訓練 VAE,用 **ELBO 值當異常分數** → AUROC 0.756(通關成立)
> - ELBO 分解、重參數化 vs REINFORCE(梯度變異數差 **898×**)、OOD 偵測
> - **但**:不看模型的梯度能量統計拿到 0.915,打敗 VAE 16 個百分點;
>   追查證明 ELBO 一大半在測影像複雜度,扣掉後救回 0.881(+0.125)
> - 四個負面結果:IWAE 收緊下界無效(0.2σ)、posterior collapse 到 0 個活躍單元
>   AUROC 反而最高、latent 容量 d≥8 即飽和

---

## 8 · 怎麼把這些放進履歷

> [!important] 這一節可能比技術部分更重要

### 8.1 每個專案的 GitHub 該有什麼

```
project-name/
├── README.md          ← 最重要的檔案(見下)
├── notebooks/
│   ├── 01_eda.ipynb
│   ├── 02_model.ipynb
│   └── 03_results.ipynb
├── src/               ← 可重用的程式碼(不要全部塞在 notebook)
├── figures/           ← 關鍵圖表
└── requirements.txt   ← 別人能重現
```

### 8.2 README 的黃金結構

```markdown
# 專案標題

## 一句話
[問題] → [方法] → [結果]
例:「用貝葉斯決策理論,把心臟病診斷模型的告警門檻從預設的 0.5
     優化到 0.03,在漏診代價為誤診 30 倍的假設下,將期望損失降低 62%。」

## 為什麼這個問題需要貝葉斯?
[兩三句,說明點估計為什麼不夠]

## 關鍵結果
[2-3 張圖,每張配一句解讀]

## 方法
[簡潔,不要抄教科書]

## 限制與誠實的部分
[這一節最能體現專業度 —— 見下]

## 如何重現
```

### 8.3 三個讓作品「看起來很專業」的元素

> [!tip] 這三件事,大部分人的作品集都沒有
>
> **1. 誠實的限制章節**
> 「MC Dropout 的校準品質不佳,ECE = 0.08,顯著高於 Deep Ensemble 的 0.03。這代表在需要精確機率的應用中,應優先考慮 Ensemble。」
> —— **敢寫出自己方法的弱點,比任何漂亮數字都有說服力**
>
> **2. 敏感度分析**
> 「將先驗方差從 2.5 放寬至 10,貝氏因子從 $10^{3.2}$ 降至 $10^{1.8}$,結論的強度顯著下降(Lindley 悖論)。」
> —— **展示你知道自己的結論站在什麼假設上**
>
> **3. 對照基準**
> 每個貝葉斯方法都配一個簡單基準(頻率派 / plug-in / 隨機搜尋)。
> —— **沒有對照的結果,說服力接近零**

### 8.4 履歷條目的寫法

❌ **不要這樣寫**:
> 「使用 PyMC 實作貝葉斯邏輯迴歸於心臟病資料集」

✅ **要這樣寫**:
> 「建立醫療診斷的貝葉斯決策系統:量化模型的認知不確定性(MC Dropout),
> 並結合不對稱損失函數推導最優告警門檻。在漏診:誤診 = 30:1 的成本假設下,
> 相較預設門檻降低 62% 期望損失,並自動將 12% 的高不確定性病例導向人工複核。」

**差別**:第一個寫「你用了什麼工具」,第二個寫「你解決了什麼問題、達成什麼效果」。

### 8.5 建議的作品集組合

> [!tip] 不需要做完全部九個專案
>
> **最小可行組合(三件作品,涵蓋不同能力)**:
> 1. **A1 醫療決策** —— 展示決策理論與商業/臨床思維
> 2. **B1+B2 系外行星** —— 展示嚴謹的科學建模與 MCMC 功力
> 3. **D1 A/B 測試** —— 展示業界應用與溝通能力
>
> **如果要加第四件** → **C1 階層模型**(概念最深,而且很多人不會)

---

## 9 · 常見陷阱

> [!bug] 數值問題
> - **永遠在 log 空間算機率**,用 `scipy.special.logsumexp`
> - 協方差矩陣求逆 → 改用 **Cholesky 分解**(`scipy.linalg.cho_solve`),數值穩定得多
> - GP 的 $K$ 矩陣要加 jitter:`K += 1e-6 * np.eye(n)`

> [!bug] 貝葉斯特有的陷阱
> - **對正數參數用了高斯先驗** → 機率漏到負數區。改用 `HalfNormal` / `LogNormal` / `Gamma`
> - **MCMC 沒收斂就用**:一定要看 $\hat R < 1.01$ 和 `divergences == 0`
> - **PyMC 出現 divergences** → 通常是階層模型的 funnel 問題。解法:改用 **non-centered parameterization**
>   ```python
>   # 中心化(容易 diverge)
>   a = pm.Normal("a", mu_a, sigma_a, shape=n)
>   # 非中心化(建議)
>   a_raw = pm.Normal("a_raw", 0, 1, shape=n)
>   a = pm.Deterministic("a", mu_a + sigma_a * a_raw)
>   ```
>   > 🔑 **這就是重參數化技巧**(主題五第三卡)—— 在 MCMC 裡它同樣救命

> [!bug] 領域特有的陷阱
> **醫療**
> - 類別極度不平衡 → **不要用 accuracy**,用 AUROC / AUPRC / 期望損失
> - 資料洩漏:同一病人的多筆記錄必須在**同一個 split** 裡
> - MC Dropout 測試時要 `model.train()`,但這也會開啟 BatchNorm → **改用 LayerNorm 或 GroupNorm**
>
> **天文**
> - 光曲線要先 detrend,否則恆星自轉的變化會蓋掉凌日訊號
> - 臨邊昏暗參數有物理上的有效範圍,先驗要正確編碼(用 Kipping 參數化)
> - 週期參數的後驗常有**多個峰**(倍週期別名)—— MCMC 可能卡住,要多鏈檢查
>
> **流行病學**
> - 通報延遲會讓最近幾天的資料**系統性偏低** —— 建模時要截掉或顯式處理
> - 週末效應:很多國家週末通報數會掉 —— 需要週期性項
>
> **A/B 測試**
> - 新奇效應:剛上線的前幾天行為會異常
> - 多重比較:同時測 10 個變體,假陽性率會暴增

---

## 10 · 進度追蹤

| 領域 | 專案 | 難度 | 預估 | 履歷價值 | 狀態 |
|---|---|:-:|---|:-:|:-:|
| **暖身** | 0 · 環境測試 | ⭐ | 30 分 | — | ✅ |
| **A 醫療** | A1 · 診斷決策系統 ⭐ | ⭐⭐⭐ | 2–3 天 | 🔥🔥🔥 | ✅ |
| | A2 · 貝葉斯生存分析 | ⭐⭐⭐ | 2–3 天 | 🔥🔥 | ✅ |
| | A3 · 影像不確定性 / OOD | ⭐⭐⭐⭐ | 3–4 天 | 🔥🔥🔥 | ✅ |
| **B 天文** | B1 · 凌日參數估計 | ⭐⭐⭐⭐ | 3–4 天 | 🔥🔥🔥 | ✅ |
| | B2 · 貝氏因子模型比較 | ⭐⭐⭐⭐⭐ | 2–3 天 | 🔥🔥🔥 | ✅ |
| **C 公衛** | C1 · 階層模型 ⭐ | ⭐⭐⭐ | 2 天 | 🔥🔥 | ✅ |
| | C2 · 疾病傳播建模 | ⭐⭐⭐⭐ | 3–4 天 | 🔥🔥 | ✅ |
| **D 商業** | D1 · 貝葉斯 A/B 測試 | ⭐⭐ | 1–2 天 | 🔥🔥🔥 | ✅ |
| **E 實驗設計** | E1 · 貝葉斯最優化 | ⭐⭐⭐⭐ | 3–4 天 | 🔥🔥 | ✅ |
| **補充** | S1 · VAE 異常偵測(補主題五) | ⭐⭐⭐⭐ | 2 天 | 🔥🔥 | ✅ |

> [!success] 全部 10 個專案完成
> 理論覆蓋檢核表(§7)的**主題五(變分推論 / ELBO)**由 S1 補上,七個主題全數涵蓋。

### 建議順序

```
0 (暖身)
  ↓
D1 (A/B 測試) ← 最快看到成果,建立信心
  ↓
A1 (醫療決策) ← 核心作品,呼應課堂
  ↓
C1 (階層模型) ← 概念收穫最大
  ↓
B1 → B2 (天文) ← 最硬的作品
  ↓
A3 (OOD) / E1 (BO) ← 依興趣選
```

> [!tip] 為什麼先做 D1?
> 它只需要 Beta-Binomial 共軛(封閉解,不用 MCMC),一兩天就能做出完整成果。
> **先有一件完成的作品,對後續動力極重要。**

---

## 附錄:每個專案的「一句話目的」

| 專案 | 一句話 |
|---|---|
| **A1** | 把「90% 是惡性」變成「該不該開刀」 |
| **A2** | 讓「還活著的病人」也能貢獻資訊 |
| **A3** | 讓模型知道自己沒見過這種影像 |
| **B1** | 從 0.02% 的亮度凹陷,推論出一顆行星的大小 |
| **B2** | 用邊際似然判定「這到底是不是行星」 |
| **C1** | 讓小樣本的群組向大樣本借力 |
| **C2** | 從確診數反推傳染率,並誠實說出不確定性 |
| **D1** | 回答老闆真正想問的「B 比 A 好的機率是多少」 |
| **E1** | 用後驗決定下一個實驗做在哪 |

---

> [!quote] 最後
> 這九個專案表面上是九個不同領域。
>
> **但它們的骨架是同一條式子:$p(\theta\mid D) \propto p(D\mid\theta)\,p(\theta)$。**
>
> **當你能在履歷上展示「同一套數學,五個領域」,那比展示五個不同的技巧更有力量 ——**
> **因為前者證明你理解了原理,後者只證明你會用工具。**
